// Public API of the situation gate's pure logic (no React/DOM/OCR here — see
// components/checklist/SituationGate.tsx for the wiring, and ./extractLetterText for the
// client-only letter-reading helper). Ported from index.html.

export * from './types';
export * from './routing';
