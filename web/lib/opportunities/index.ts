// Public API of the funded-opportunities directory (pure logic + static data only — no
// React/localStorage; see web/app/opportunities/page.tsx for the wiring). Ported from index.html.

export * from './types';
export * from './store';
